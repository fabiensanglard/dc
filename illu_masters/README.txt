Here are the drawing "source of truth". All authoring should be done on these files.

Once SVGs are ready, you should run the script "publish.sh" which convert all objects
in the SVGS to path. This is done in order to avoid issues if the rendering browser
does not have the font required (a.k.a: Roboto).
